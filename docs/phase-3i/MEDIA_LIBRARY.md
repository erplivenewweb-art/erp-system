# Media Library

The media library includes labelled search and folder filtering, three reserved media cards, preview controls and a disabled upload action. Cards use local CSS placeholders and meaningful accessible names.

No file picker action, upload backend, external image, CDN call, media metadata or persistent folder exists.
