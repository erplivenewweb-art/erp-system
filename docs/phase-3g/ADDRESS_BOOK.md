# Address Book

The address experience includes a synthetic default-address card, edit/delete placeholders, labelled add-address controls, default choice, save placeholder and an accessible no-addresses state.

No real person, street, city, region or postal code is displayed, stored or transmitted. The form uses existing shared controls and remains non-submitting.
