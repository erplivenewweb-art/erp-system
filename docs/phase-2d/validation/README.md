# Static Blueprint Validation

Run with Python 3:

```powershell
python docs/phase-2d/validation/validate_blueprint.py
```

The dependency-free validator confirms one selected stack/styling strategy, five route groups, 46 modules, eleven testing layers, ten fully bounded Phase 3 subphases, twelve exact diagrams, browser/internal-API prohibition, required quality domains and all four Product Bible files referencing Phase 1A–2D. It rejects runtime-looking frontend, SQL and environment files in both allowed output directories.

It accesses no network, package manager, database, ERP process, migration, runtime or deployment.

