import { Checkbox, Field, Input, Label, Select, Textarea } from "@/components/forms";
import { AccountHeader, AccountShell } from "@/features/account";
import styles from "@/features/account/Account.module.css";

export function AddressCard() { return <article className={styles.card}><div className={styles.cardHeader}><h2>Synthetic address preview</h2><span className={styles.badge}>Default preview</span></div><p>No real person, street, city or postal code is represented.</p><div className={styles.actions}><button className={styles.button} type="button">Edit address UI</button><button className={styles.button} type="button">Delete placeholder</button></div></article>; }
export function AddressForm() { return <section className={styles.card}><h2>Add address UI</h2><div className={styles.formGrid}><Field><Label htmlFor="address-name">Full name</Label><Input id="address-name" name="name" placeholder="Synthetic name" /></Field><Field><Label htmlFor="address-phone">Phone</Label><Input id="address-phone" name="phone" placeholder="Not submitted" /></Field><Field className={styles.wide}><Label htmlFor="address-lines">Address</Label><Textarea id="address-lines" name="address" placeholder="Synthetic address only" /></Field><Field><Label htmlFor="address-city">City</Label><Input id="address-city" name="city" /></Field><Field><Label htmlFor="address-region">State</Label><Select defaultValue="" id="address-region" name="region"><option disabled value="">Choose placeholder</option><option>Sample region</option></Select></Field></div><Checkbox>Make default address placeholder</Checkbox><button className={styles.button} type="button">Save address preview</button></section>; }
export function EmptyAddresses() { return <div className={styles.empty} role="status"><h2>No saved addresses</h2><p>Address persistence requires a future authenticated service.</p></div>; }
export function AddressesPage() { return <AccountShell><AccountHeader eyebrow="Address book preview" title="Addresses" description="Add, edit, default and empty states without real customer or location data." /><div className={styles.list}><AddressCard /><AddressForm /><EmptyAddresses /></div></AccountShell>; }
