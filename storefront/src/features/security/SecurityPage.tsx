import { Checkbox, Field, Input, Label } from "@/components/forms";
import { AccountHeader, AccountShell } from "@/features/account";
import styles from "@/features/account/Account.module.css";
export function PasswordCard() { return <section className={styles.card}><h2>Change password UI</h2><Field><Label htmlFor="current-password">Current password placeholder</Label><Input autoComplete="current-password" id="current-password" type="password" /></Field><Field><Label htmlFor="new-password">New password placeholder</Label><Input autoComplete="new-password" id="new-password" type="password" /></Field><button className={styles.button} type="button">Update password preview</button><p>No credential is validated, transmitted or stored.</p></section>; }
export function SecurityPage() { return <AccountShell><AccountHeader eyebrow="Security interface preview" title="Security and privacy" description="UI-only password, two-factor, sessions and privacy controls without authentication." /><div className={styles.list}><PasswordCard /><section className={styles.card}><h2>Two-factor authentication</h2><Checkbox>Enable two-factor placeholder</Checkbox><p>No secret, QR code, OTP or recovery code is generated.</p></section><section className={styles.card}><h2>Device sessions</h2><p>No live devices or sessions exist.</p><button className={styles.button} type="button">Sign out other sessions placeholder</button></section><section className={styles.card}><h2>Privacy</h2><Checkbox>Allow account-personalization placeholder</Checkbox><button className={styles.button} type="button">Request data placeholder</button></section></div></AccountShell>; }
