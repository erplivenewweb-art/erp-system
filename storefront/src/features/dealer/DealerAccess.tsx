import Link from "next/link";
import { Field, Input, Label, Select } from "@/components/forms";
import { Container, Section, Stack } from "@/components/layout";
import styles from "@/features/b2b/B2B.module.css";
const fields=[['company','Company name','text'],['contact','Contact person','text'],['phone','Phone','tel'],['email','Email','email'],['city','City','text'],['gst','GST placeholder','text']] as const;
export function DealerRegistration() { return <Section><Container><Stack gap="lg"><header className={styles.header}><span className={styles.eyebrow}>Dealer application preview</span><h1>Tell us about your business</h1><p className={styles.lede}>UI only. Submission does not create an application, dealer or GST record.</p></header><form className={styles.card}><div className={styles.formGrid}>{fields.map(([id,label,type])=><Field key={id}><Label htmlFor={`dealer-${id}`}>{label}</Label><Input id={`dealer-${id}`} name={id} placeholder="Synthetic information only" type={type} /></Field>)}<Field><Label htmlFor="business-type">Business type</Label><Select defaultValue="" id="business-type"><option disabled value="">Choose placeholder</option><option>Retailer</option><option>Distributor</option></Select></Field><Field><Label htmlFor="business-category">Business category</Label><Input id="business-category" placeholder="Synthetic category" /></Field><Field><Label htmlFor="dealer-state">State</Label><Input id="dealer-state" /></Field><Field><Label htmlFor="dealer-country">Country</Label><Input id="dealer-country" /></Field></div><button className={`${styles.button} ${styles.primary}`} type="button">Submit application preview</button></form></Stack></Container></Section>; }
export function DealerLogin() { return <Section><Container><div className={styles.empty}><span className={styles.eyebrow}>Dealer access preview</span><h1>Dealer login</h1><p>No authentication or live dealer account exists.</p><Field><Label htmlFor="dealer-login-email">Email</Label><Input id="dealer-login-email" type="email" /></Field><Field><Label htmlFor="dealer-login-password">Password</Label><Input id="dealer-login-password" type="password" /></Field><button className={`${styles.button} ${styles.primary}`} type="button">Login preview</button><button className={styles.button} type="button">Forgot password placeholder</button><Link className={styles.button} href="/dealer/register">Register interest</Link></div></Container></Section>; }
