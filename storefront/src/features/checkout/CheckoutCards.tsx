import { Checkbox, Field, Input, Label, Radio, Select, Textarea } from "@/components/forms";
import styles from "@/features/cart/CommerceFlow.module.css";

export function CheckoutCard({ children, title }: { children: React.ReactNode; title: string }) { return <section className={styles.card}><h2>{title}</h2>{children}</section>; }
export function AddressCard() { return <CheckoutCard title="Shipping address"><div className={styles.formGrid}><Field><Label htmlFor="full-name">Full name</Label><Input autoComplete="name" id="full-name" name="fullName" placeholder="Synthetic customer name" /></Field><Field><Label htmlFor="phone">Phone</Label><Input autoComplete="tel" id="phone" inputMode="tel" name="phone" placeholder="Not submitted" /></Field><Field className={styles.wide}><Label htmlFor="address">Address</Label><Textarea autoComplete="street-address" id="address" name="address" placeholder="Synthetic address only" /></Field><Field><Label htmlFor="city">City</Label><Input autoComplete="address-level2" id="city" name="city" /></Field><Field><Label htmlFor="region">State</Label><Select defaultValue="" id="region" name="region"><option disabled value="">Choose a placeholder state</option><option>Sample region</option></Select></Field><Field><Label htmlFor="postal-code">Postal code</Label><Input autoComplete="postal-code" id="postal-code" inputMode="numeric" name="postalCode" /></Field></div><Checkbox name="billingSame">Billing address is the same as shipping</Checkbox></CheckoutCard>; }
export function ShippingCard() { return <CheckoutCard title="Shipping method"><div className={styles.optionList}><Radio defaultChecked name="shipping">Standard shipping — timing and price pending</Radio><Radio name="shipping">Priority shipping placeholder — unavailable</Radio></div></CheckoutCard>; }
export function PaymentCard() { return <CheckoutCard title="Payment method"><div className={styles.optionList}><Radio defaultChecked name="payment">Payment method will be selected in a future secure gateway</Radio><Radio disabled name="payment">Cash option placeholder — not approved</Radio></div><p>No card, bank or payment credentials are collected.</p></CheckoutCard>; }
export function ExtrasCard() { return <CheckoutCard title="Order notes"><div className={styles.formGrid}><Field><Label htmlFor="coupon">Coupon code</Label><Input id="coupon" name="coupon" placeholder="UI only" /></Field><Field className={styles.wide}><Label htmlFor="gift-note">Gift note</Label><Textarea id="gift-note" maxLength={240} name="giftNote" placeholder="Not saved or submitted" /></Field></div></CheckoutCard>; }
