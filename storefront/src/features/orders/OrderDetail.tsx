import { AccountHeader, AccountShell, syntheticOrders } from "@/features/account";
import styles from "@/features/account/Account.module.css";

export function OrderTimeline() { const stages = [["Preview created", "No order event occurred"], ["Processing placeholder", "No manufacturing or inventory status is connected"], ["Delivery placeholder", "No shipment or tracking exists"]]; return <ol aria-label="Order timeline preview" className={styles.timeline}>{stages.map(([title, detail]) => <li key={title}><strong>{title}</strong><span>{detail}</span></li>)}</ol>; }
export function OrderDetailPage() { const order = syntheticOrders[0]; return <AccountShell><AccountHeader eyebrow="Synthetic order detail" title="Order detail preview" description="No order number, payment, invoice, shipment or customer record exists." /><div className={styles.grid}><section className={styles.card}><h2>Order summary</h2><span className={styles.badge}>{order.status}</span><dl className={styles.summary}><div><dt>Reference</dt><dd>Not generated</dd></div><div><dt>Total</dt><dd>{order.total}</dd></div><div><dt>Shipping</dt><dd>Not selected</dd></div><div><dt>Payment</dt><dd>Not collected</dd></div></dl></section><section className={styles.card}><h2>Address placeholders</h2><p>Shipping address: Not stored</p><p>Billing address: Not stored</p></section><section className={styles.card}><h2>Timeline</h2><OrderTimeline /></section><section className={styles.card}><h2>Notes and support</h2><p>Order notes placeholder — no note was submitted.</p><div className={styles.actions}><a className={styles.button} href="/contact">Contact support</a><button className={styles.button} type="button">Download invoice placeholder</button></div></section></div></AccountShell>; }
