import { DealerHeader, DealerShell, wholesaleProducts } from "@/features/b2b";
import styles from "@/features/b2b/B2B.module.css";
export function BulkOrderTable() { return <div className={styles.tableWrap} role="region" aria-label="Bulk order table" tabIndex={0}><table className={styles.table}><caption>Static bulk-order quantity preview</caption><thead><tr><th>Product shell</th><th>MOQ</th><th>Quantity</th><th>Pricing</th></tr></thead><tbody>{wholesaleProducts.map((name)=><tr key={name}><th scope="row">{name}</th><td>Pending quotation</td><td><input aria-label={`Quantity for ${name}`} defaultValue="0" inputMode="numeric" /></td><td>Not exposed</td></tr>)}</tbody></table></div>; }
export function BulkOrderPage() { return <DealerShell><DealerHeader eyebrow="Bulk enquiry preview" title="Build a bulk enquiry" description="Quantity controls are UI-only. No MOQ validation, calculation, stock check or submission occurs." /><p className={styles.privateBanner}>MOQ notice: minimum quantities and availability are confirmed only in an approved future quotation.</p><section className={styles.card}><BulkOrderTable /><button className={`${styles.button} ${styles.primary}`} type="button">Submit bulk enquiry preview</button></section></DealerShell>; }
